export type Option = {
  title: string;
  icon: string;
  color1: string;
  color2: string;
  beats: string;
};

export enum Outcome {
  youWin = "You win",
  draw = "Draw",
  houseWins = "House wins",
}
